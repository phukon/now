import localFont from "next/font/local";

// ---- local ----

export const localEbgaramond = localFont({
  src: "./EBGaramond-VariableFont_wght.ttf",
  variable: "--local-ebgaramond",
});

export const localBasteleur = localFont({
  src: "./Basteleur-Moonlight.ttf",
  variable: "--local-basteleur",
});